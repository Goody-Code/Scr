module.exports = {
  testEnvironment: 'node',
  verbose: true,
  // Optional: clear mocks between every test
  clearMocks: true,
  // Optional: setup file after env
  // setupFilesAfterEnv: ['./tests/setupTests.js'], // if you need global setup for tests
};
